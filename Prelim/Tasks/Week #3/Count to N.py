##############
# Count to N #
##############

countTo = eval(input("Let's count! What number should we count to?\nNumber: "))
currentCount = 0
while currentCount < countTo:
    currentCount += 1
    print(str(currentCount))
